name: Fetch Posts from Content Repo (disabled)

on: {}

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Run fetch script (disabled)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CONTENT_TOKEN: ${{ secrets.CONTENT_TOKEN }}
          CONTENT_REPO: joshualinog/scribal.bible-CONTENT-CREATION
          POST_LABELS: POST,isPost
        run: |
          echo "Workflow disabled: fetch-posts.yml"

      - name: Commit generated JSON (disabled)
        run: |
          echo "Workflow disabled: fetch-posts.yml"
